# 新机部署与使用记录

> 状态：已确认（新机部署及 OP 测试包交付完成）
> 最后更新：2026-09-10
> 适用范围：Mac mini 私有化客户端打包机
> 关联文档：[执行规划](2026-09-09-私有化客户端打包机执行规划.md)、[实现与验收记录](2026-09-09-私有化打包机实现与验收记录.md)

## 2026-09-10 最新部署与身份接线

当前接手信息以[说明](../说明.md)为准，包含用户要求记录的两机 SSH 地址、账户密码和本人密钥。本节覆盖下方早期统一 token、Windows Runner 计划和首次验证版本结论；历史故障与证据原样保留。

Windows x64 已在同一台新 Mac 成功构建并上传：任务 `bed7f67d-d5b0-4dcf-aa41-e16132f582fd`，OP 测试配置，`private/260907` 固定 `62940c5cb5503be3824bcee5771773c42b226241`，版本 `1.0.0-win64.5`，快速未签名。最终 EXE 435,033,327 字节，SHA-256 `00048defa2ac20823afe9c6a096ade134d32d518eb052ce0264378f774df3901`。共享盘 `/Volumes/Share/private/windows/op-validation-1.0.0-win64.5-win-x64-quick-bed7f67d-d5b0-4dcf-aa41-e16132f582fd.exe`。构建/上传约 5 分 23 秒；安装业务验收仍 pending。

身份已切换到 SaaS 实时验证，提交 `c0753b1284` 已部署。之前复制密钥名单后无法识别新人，是副本过期；这次不复制名单，所有请求均查询旧机 `/api/identity/unlock`。源映射仍在旧机控制台模块的 `state/packers.local`，26 人；未改 SaaS 服务或映射。

实测新机 PM2 控制台直连旧机报 `EHOSTUNREACH`，SSH 终端的 curl 和 Node 均成功；换成 Node 原生 HTTP 仍失败，因此最终保留 fetch，使用常驻 SSH 隧道解决后台网络访问。机器配置 `identityProviderUrl=http://127.0.0.1:18787`；PM2 `private-pack-identity-tunnel` 运行 `/usr/bin/ssh` 转发至 `tong@192.168.31.236` 的回环 8787，新机只监听回环 18787。

隧道专用密钥 `/Users/mini/.ssh/id_ed25519_saas_identity`，旧机 authorized_keys 添加的公钥限定 `permitopen="127.0.0.1:8787"`，禁用交互命令。SSH 配置包含 BatchMode、IdentitiesOnly、ExitOnForwardFailure、ServerAliveInterval=15、ServerAliveCountMax=3；PM2 自动重启并已 save。整机断电重启尚未实测。

实际验证：新机本人密钥 200 / 杨凯胜；Cookie 会话 200；错误密钥、旧 access-token、退出后的 Cookie 401。隔离测试覆盖 SaaS 撤销/改名、伪造姓名、断连、无效响应，模块全量 52 项通过。SaaS 不可用不回退旧 token，既有 worker 不因认证服务失联自动取消。管理员排障先用 `curl http://127.0.0.1:18787/api/identity` 检查隧道。

部署期间曾将 JSON 尾部写成字面量反斜杠 n，导致控制台启动失败，已修复；今后更新配置后先用 JSON.parse 校验再重启。后续升级仍须先确认无活动构建/上传，禁止凭 detached 参数推断不会中断。此次恢复前已确认活动任务为空。定时任务未恢复。

补充实机故障恢复验证：停止身份隧道后，控制台返回 `identity_provider_unavailable` / `ECONNREFUSED`，没有回退旧 token；重启隧道后，个人密钥恢复 200 / 杨凯胜。三个 PM2 服务已 save，原登录启动项 `~/Library/LaunchAgents/pm2.mini.plist` 已加载为 `gui/501/com.PM2`。未执行整机重启。交接 docs 保持本地更新，未将用户要求记录的凭据推送到代码仓库。

## 历史部署与故障记录（从 2026-09-09 起）

## 当前交付结果

用户追加要求已完成：新增“测试包 / 正式包”选项，默认测试包不进行 Apple 公证。正式包保留显式公证选项。

已使用用户保存的“新打包机 · OP 测试验证”配置，构建 `private/260907` 的 Community 客户端并上传共享盘：

- 源码提交：`3f102680a81741ca49dcb12f6bf4df8dfbc708c8`。
- 版本：`1.0.0-test.1`，Mac Apple Silicon，测试包（未公证，本地签名通过）。
- 成功任务：`71989352-7eb1-4b36-85bb-ae6ae6028847`，状态 `built`，上传状态 `uploaded`。
- 文件：`op-validation-1.0.0-test.1-mac-arm64-test-71989352-7eb1-4b36-85bb-ae6ae6028847.dmg`。
- 大小：527,300,530 字节（约 503 MiB）。
- SHA-256：`1f6c8593358157f7ba40caaae85e1a9e9acfc9e117e3492420bdb3e40695bf15`。
- 共享盘位置：`\\192.168.31.40\Share\private\`；新机挂载路径 `/Volumes/Share/private/`。
- 本地归档仍在构建目录的 `apps/tabtin-electron/dist-app-quick/`。

已挂载最终 DMG，验证包内版本、应用身份、五项编译地址和本地签名；共享盘传输校验通过，下载接口局域网直连返回 200。未执行客户登录/业务联调，也未做 Intel/Windows 真机验收。

保存的地址保持原样：API 与 IM 均为 `http://192.168.31.230:6060/api`，协作为 `ws://192.168.31.230:4100`，实时消息为 `ws://192.168.31.230:8100/connection/websocket`，分享页为 `https://web-test.tabtin.com`。

先前 SaaS 公证验证任务已按用户要求取消。第一次 OP 构建（`99677746-5642-47c3-806b-8edc7ad02d88`）在旧脚本调用 Finder 时失败；已修复为测试包采用无 Finder 的命令行封装，修复后的第二次构建成功。失败记录和构建适配器证据均保留，没有把首次失败写成成功。

控制台代码已推送到 `codex/private-pack-machine`，执行逻辑版本为 `c5a53e1b2d`（随后文档更新至 `e99c60f7be`）；客户端源码仍严格使用上述 private 分支提交。新增测试现为 29 项通过。构建完成后 Git 工作目录为 clean，临时 DMG 助手已恢复。

## 最新授权与方向

用户要求直接在新机完成部署，不再停在本地预览；保留现有 SaaS 的日常操作顺序：选分支 → 拉代码 → 同步依赖 → 构建 → 签名公证 → 按需上传。用户明确授权复用旧机的 Git 拉取凭据和 SaaS 签名、公证材料。此文不保存系统密码、私钥内容、Apple 密钥内容或控制台访问令牌。

原有“每次手工 checkout 和安装依赖”的设计已修正。构建目录初始化一次，此后由程序自动 fetch、按已确认 SHA 检出、执行旧 SaaS 同款 pnpm 安装策略。排队过程中分支推进不会改变已经确认的构建提交。未提交修改会明确报错，不自动 stash/reset。

## 旧 SaaS 机器实查

通过用户提供的 `tong@192.168.31.236` 核查，未触发或重启旧 SaaS 打包服务。

| 内容 | 实际情况 |
| --- | --- |
| 控制台 | `192.168.31.236:8787`，主机 `1deMac-mini.local` |
| Mac Runner | 实际为 `192.168.31.236:8788`；不是此前转述的 `192.168.31.200` |
| 控制台源码 | `/Users/tong/workspace/work/TabTin-pack-gateway-runtime/tools/tabtin-pack-gateway` |
| 生效配置 | `state/console.mini.json`、`state/runner-mac.mini.json`，不是默认 config 文件 |
| 构建源码 | `/Users/tong/workspace/work/TabTin-pack-runner` |
| Node / pnpm | `/Users/tong/opt/node-v22.17.0-darwin-arm64/bin/`；Node 22.17.0，pnpm 9.15.0 |
| 依赖 | 构建根 `node_modules` 与 `apps/tabtin-electron/node_modules` 均存在 |
| 拉代码 | `prepareClientPackageBuild` 按所选分支同步，配置为 ff-only |
| 同步依赖 | `ensureWorkspaceDependencies` 在构建根运行冻结锁文件安装，Mac 使用 prefer-offline 和 Electron 缓存 |
| 共享盘 | `//guest@192.168.31.40/Share` 挂载到 `/Volumes/Share`；`~/.tabtin-pack-share` 是指向它的符号链接 |

旧机钥匙串包含两个身份，只选择其中一个 Developer ID Application 导出，没有整体导出钥匙串。SSH 会话无法弹授权窗口，最终通过临时桌面会话任务发起，用户在旧机确认后导出成功；没有修改旧机打包服务配置或撤换原证书。

## 新机实际目录

主机：`minideMac-mini.local`，账号 `mini`，地址 `192.168.31.230`。

| 用途 | 新机路径 |
| --- | --- |
| 控制台网址 | `http://192.168.31.230:8787` |
| 控制台源码 clone | `/Users/mini/workspace/work/TabTin-private-pack-gateway-runtime` |
| 控制台程序 | 上述目录的 `tools/tabtin-private-pack-gateway` |
| 构建源码 clone | `/Users/mini/workspace/work/TabTin-private-pack-runner` |
| 机器配置 | `/Users/mini/.config/tabtin-private-pack/machine.json` |
| 启动脚本 | `/Users/mini/.config/tabtin-private-pack/start.sh` |
| 任务/配置/证据 | `/Users/mini/.tabtin-private-pack/` |
| Node / pnpm / PM2 | `/Users/mini/opt/node-v22.17.0-darwin-arm64/bin/` |
| Go | `/Users/mini/opt/go/bin/`，Go 1.26.1 |
| 签名、公证配置 | `/Users/mini/.config/tabtin-codesign/secrets.env` |
| 独立签名钥匙串 | `/Users/mini/Library/Keychains/tabtin-private-pack.keychain-db` |
| Mac 安装包 | 构建 clone 下 `apps/tabtin-electron/dist-app-quick/` |
| 私有共享目录 | `/Volumes/Share/private/`；Windows 子目录 `private/windows/` 已创建 |

控制台与构建目录分开，源码切换不会切走正在运行的控制台。两个 clone 的依赖已初始化；第二份利用 pnpm 缓存，安装约 30 秒。

控制台曾切换到 SaaS 个人密钥，现已按用户追加要求恢复原统一 token（详见文末回退记录）。当时新机映射副本为 `/Users/mini/.config/tabtin-private-pack/packers.local`（权限 600），机器配置通过 `packersFile` 指向它。源文件来自旧控制台的 `state/packers.local`，24 个人员映射原样复用，未生成新密钥。旧 `access-token` 及本机副本仍保留，但个人密钥模式下不再允许用它解锁。源文件后续增删人员时，需要同步这个副本；尚未实现自动同步。文档不记录密钥内容。

## 已完成的机器准备

- 安装 Command Line Tools for Xcode 16.2；保留 macOS 15.2，没有升级系统。
- 官方 Node 与 Go 下载归档经 SHA-256 核验；安装 pnpm 9.15.0、PM2 7.0.3。
- 按用户授权复用旧机 Git 身份，保存为新机独立 SSH 文件，并限定 GitHub 主机配置；GitHub 拉取验证成功。
- 安装指定 Developer ID 到新机独立钥匙串，配置 unattended codesign 权限；真实小程序签名及严格验签成功。
- Apple notarytool API 访问验证成功；迁移配置只包含 Apple 相关字段，没有复制旧 Sentry 上传令牌。
- 挂载既有 Share，创建 private 子目录，已完成临时文件写入检查；原 SaaS 包未清理。
- PM2 启动 `private-pack-console`；`private-pack-awake` 保持执行机运行时不进入空闲睡眠。
- 配置 PM2 的用户登录自启动；实际载入 `gui/501/com.PM2`。这不是自动登录配置，机器重启后仍按 macOS 原有登录策略进入用户会话。

## 代码与检查

实现已提交并推送至 `codex/private-pack-machine`，提交 `a4d679551283047d0cb8011546e0f45ff549aefa`。本地同名 worktree 已快进到同一提交。没有合并到 release 或 main。

新机已执行：

- 新模块 24 项测试通过。
- 既有 Windows 签名/快捷方式测试通过。
- 完整 Electron workspace 依赖构建通过。
- 提交钩子的导出检查、修改文件 ESLint 和 Electron typecheck 通过；ESLint 存在既有 any 警告，但没有阻断错误。

日志位于新机 `/Users/mini/opt/downloads/` 的 `runtime-install.log`、`source-prepare.log`、`workspace-build.log`、`typecheck.log`、`private-tests.log`、`commit.log`、`push.log`。源代码与证书校验信息不要与应用打包成功混为一谈。

## 首次 SaaS 验证的历史记录（已取消）

已从新控制台创建任务：

- 任务号：`0792d6e6-ba10-40ea-84ec-731dc7ca10e8`。
- 客户环境：`saas-validation`，使用 SaaS 测试 API/IM、协作、实时和分享页地址。
- 版本：`1.0.0-beta.1`；目标：Mac arm64；交付：打包后上传 private 目录。
- 源码：固定上述 `a4d6795512` 提交。
- 已经过源码同步、依赖同步、完整客户端编译；后续按用户要求取消，改为上方记录的 OP 不公证测试包。

任务完整证据在 `/Users/mini/.tabtin-private-pack/jobs/<任务号>/`，以 `job.json`、`build.log`、`package-audit.json`、`audit.json`、`delivery.json` 的实际结果为准。只有最终制品审计及签名公证通过，才能记录为已出包；上传与真机安装另记。

## 日常怎么用

1. 打开新控制台，用访问令牌登录。
2. 保存客户的 API、IM、协作、实时、分享页地址；IM 必须显式填写。
3. 选择包含私有构建接入的分支，填写版本与目标架构。
4. 点击“拉取代码并生成计划”，核对地址、提交和输出位置。
5. 选择打包方式（默认不公证测试包）并开始，页面依次显示拉取源码、同步依赖、构建、审计和上传。
6. 下载包或从 Share/private 取包；失败先看该任务日志，不手工覆盖源码或把旧 SaaS 目录当作构建根。

Windows 执行端尚未单独部署，不把新 Mac 上显示的平台选项等同于 Windows 真机可用。旧客户业务兼容性和服务端仍不在本轮部署范围内。


## 2026-09-09 追加确认：身份与 Windows 分工

用户确认：复用 SaaS 个人密钥与操作人机制；Windows 使用 `192.168.31.40` 原 Windows 主机；暂不并行；保留 `private/260907` 的 Community 应用名称。

已部署提交 `45a193bf26`：服务端由密钥识别操作人，页面显示当前姓名并支持切换。新任务保存 `triggeredBy`，手动上传保存 `uploadTriggeredBy`；历史任务不追认姓名。个人密钥只在服务端内存会话内使用，不写入任务文件和浏览器持久存储。远端私有执行端将独立重新验证相同密钥，不信任客户端填写的姓名。

本地模块 31 项测试通过；新机 HTTP 契约 4 项通过。部署后逐一验证 24 个既有个人密钥能解析成原姓名，退出后会话失效，共用 token 返回 401。未修改 SaaS 源映射。

Windows：8788 服务可达，22 端口不可达；旧 Mac SSH 配置没有 Windows 系统登录。已向用户请求 Windows SSH 账户与授权方式。尚未部署 Windows 私有化执行端，不能把原 SaaS 的 8788 API 当成新执行端协议；Community Windows 和 ia32 的适配及实际出包仍待完成。

部署意外与恢复：更新控制台时，PM2 连同独立构建进程一起终止，Intel 任务 `7947a507-cfd7-4726-b238-e520c42d50d4` 被标记 interrupted，原日志保留。确认工作进程已死亡、源码锁属于该任务、临时 DMG helper 与证据副本一致后，只恢复该文件并归档释放原锁。使用原计划（原 SHA、客户地址、版本、Intel 架构、测试包、上传方式均保留）创建恢复任务 `90169f70-291f-4b8f-aacb-be23fc89e854`，操作人明确标记“部署恢复（原操作人未记录）”，未冒认某个人。

后续升级控制台必须等待构建和上传队列空闲，不能根据 detached 参数推断 PM2 重启不会杀子进程。恢复任务完成状态与共享盘结果待补记。


### 追加回退：恢复统一 token

用户反馈旧机新增个人密钥没有同步到新机，确认先恢复此前统一 token。当前事实：原 `access-token` 已重新可用，实测返回 200，操作人显示“共用令牌（未识别）”；原个人密钥返回 401。未生成新 token，原本机副本仍可使用。

为避免再次中断 Intel 构建，没有重启控制台。先备份原 SaaS 映射到新机配置目录 `packers.saas-backup.local`（600），再利用现有请求时重读映射机制，让当前进程仅接受统一 token；机器配置同时移除 `packersFile`，下次安全重启后直接走 `tokenFile`。旧机原映射未改动。页面入口改为“控制台访问密钥 / 令牌”，部署源码 `59964f5292`。

个人身份功能代码保留，但当前不启用。历史已经记录的姓名保留，新建任务明确记录共用凭据，不能据此断言实际操作人。此后如需再开启个人密钥，应先解决映射同步，再切换认证模式。


恢复补充：`90169f70-291f-4b8f-aacb-be23fc89e854` 完成签名与封装后，旧 Community 的 x64 启动冒烟因缺少 Rosetta 报 `spawn Unknown system error -86`。首次 Rosetta 安装因远程 locale 导致 postinstall 失败；显式 `LANG=C LC_ALL=C` 重试成功，`arch -x86_64 uname -m` 返回 x86_64。未升级 macOS。

确认无活跃构建/上传后，安全重启控制台，正式使用原 `tokenFile`；热切换的过渡映射不再生效。Intel 恢复任务改为 `59c994be-da4a-477f-a27c-6689af0751ee`，关联上一次失败记录，使用同一计划与明确的部署恢复操作人。等待构建验收和上传结果。


### Intel 恢复完成

任务 `59c994be-da4a-477f-a27c-6689af0751ee` 已完成，`status=built`、`uploadStatus=uploaded`。已通过旧脚本启动冒烟、最终 DMG 挂载后身份/地址/签名审计，并由上传器校验 SHA-256。文件为 `op-validation-1.0.0-test.1-mac-x64-test-59c994be-da4a-477f-a27c-6689af0751ee.dmg`，395565189 字节；SHA-256 为 `5fe50b2676cf478d13332d4bba178b2e3216386420a33c20e4aa0efaa212eff2`。位置仍是 `\\192.168.31.40\Share\private\`，对应 `/Volumes/Share/private/`。这是 Apple Silicon 上经 Rosetta 的启动验证，不代表已完成 Intel 真机和客户服务联调。

用户随后要求不再引入 Windows 打包机，改为先调研在本 Mac 生成 Windows 安装包。此前 Windows 登录方式请求及执行端部署计划暂不推进；跨平台可行性结论另写调研文档。


## 正式 Mac arm64 签名故障与修复

原正式任务 `27cc7b56-cbf5-49c4-9339-b075fab4d4cd` 在 2026-09-09 19:49 失败，electron-builder 阶段耗时 1366 秒。失败来自 codesign：`Electron Framework.framework/Electron Framework: bundle format is ambiguous (could be app or framework)`。没有进入 Apple 公证；先前耗时不是公证队列。

已在新机对 Electron 缓存检查：Framework 的 `Resources`、`Libraries`、`Versions/Current` 和主二进制别名均不再是符号链接。将同一 Framework 复制到诊断目录后，`codesign --force --sign -` 复现完全相同的 ambiguous 错误；运行客户分支既有 `repair-macos-framework-links.sh` 后，同命令及严格验签通过。探针证据在新机 `~/.tabtin-private-pack/diagnostics/framework-signing-20260909/`。没有直接修改或删除用户的源缓存。

修复提交 `2629fcd4c0` 已部署：在 Community 的 afterPack 原 hook 运行后、electron-builder 签名前，调用该分支已有的 Framework 链接修复脚本，保留 fuse 修改与导出行为。控制台保存 original/used hook 证据，结束后恢复客户源码。测试包原有修复位置在 builder 之后，无法覆盖正式签名，因此这次将相同修复能力接到签名前。

额外探针发现专用钥匙串会锁定；未解锁时 Developer ID 探针报 errSecInternalComponent，显式解锁后成功。机器配置新增专用钥匙串及密码文件路径；每次正式构建前解锁，并设置六小时超时，只操作本打包机的专用钥匙串，不改变系统登录钥匙串。密码不进入日志或任务 JSON；任务文件仅记录路径。

本地 33 项测试通过；新机针对构建模式与 hook 的 6 项测试通过。实际 Framework 的 ad-hoc 修复前失败/修复后成功、Developer ID 解锁后签名成功均已验证。

确认队列空闲后重启控制台，创建原计划重试任务 `887aaff7-490f-4f4d-b348-6bc6ad94c9ad`，关联原失败任务，客户配置、源码 SHA、版本 1.0.0、Mac arm64、正式签名公证及上传方式不变。完整正式包结果待补记。用户要求暂停的自动化 mac-windows 保持 PAUSED，本次为当前任务主动修复与验证。


正式重试进展：`887aaff7-490f-4f4d-b348-6bc6ad94c9ad` 已越过原 Framework 故障点并完成完整应用 Developer ID 签名、DMG 封装及原客户端制品审计。签名过程中出现一次重试提示，随后成功；该次瞬时重试原因未单独输出，未将其归因为证书或网络。对实际构建 Framework 保持原目录名称的副本复验，使用相同证书和 entitlements 签名也成功。

最终 DMG 已提交 Apple 公证，提交编号 `4f87e956-4859-451a-8030-4882d2a3c021`，Apple API 查询状态 `In Progress`。旧脚本 afterSign 因未注入 Apple 凭据跳过应用级公证，而网关 `notarizeCommunityDmg()` 从专用配置载入凭据提交最终 DMG；因此该“跳过”日志不代表正式包取消公证。仍待公证票据、最终包内验签和共享盘上传结束。


### 公证拒绝的第二处根因：Python 归档内部未签名

Apple 提交 `4f87e956-4859-451a-8030-4882d2a3c021` 返回 Invalid。详细报告保存在任务 `887aaff7-490f-4f4d-b348-6bc6ad94c9ad/apple-notary-log.json`：217 条问题全部位于 `native/tabtin-python-runtime/tabtin-python-runtime.tar.gz` 内，涉及 92 个 Mach-O 文件；108 条缺安全时间戳、95 条没有有效 Developer ID、13 条未签名、1 条 Python 可执行程序未启用 hardened runtime。外层 app 验签通过不意味着压缩归档内部也已签名。

提交 `1d4a3b5c0e` 已部署：正式 Mac Community afterPack 在外层签名前解包交付副本的 Python runtime，使用专用 Developer ID 和现有 entitlements 签署并严格验签每个 Mach-O，重建归档、同步 manifest 的 sha256/size、保留 manifest 权限及原运行时缓存。同时改用结构化 Apple 公证结果；仅 Accepted 才 staple，Invalid 保存完整报告并显示实际拒绝原因，避免表面只报票据错误65。新机全量35项测试通过。

为避免重复编译，创建有明确来源的重封装任务 `d58a087e-cdc4-4d3a-814d-2046cb012fc3`（retryOf 指向上述失败任务，recoveryMode=repack-signed-python）。持有专用源码锁，从同SHA已签名app复制独立交付副本，签署Python归档，重新签署外层app并严格验签，重新封装DMG，再走同一个 collectBuiltArtifact()/公证/上传链路。原失败DMG归档到原任务的 `rejected-original.dmg`；原app和缓存保留。一次性恢复脚本及进程日志留在新机 diagnostics/repack-python.mjs、repack-python-process.log。

实机已确认92个归档内程序签名/验签成功，并更新摘要；最终公证和共享盘上传结果待补记。


### 正式包最终交付与上传故障恢复

重封装任务 `d58a087e-cdc4-4d3a-814d-2046cb012fc3` 已通过 Apple 公证：提交 `e245ed88-6dc7-44db-b1d2-93bbcce9e73e` 状态 Accepted。公证票据写入及验证、挂载最终 DMG 的地址/身份/严格验签均通过。签名后的 Python 3.12.13 实际启动成功，zmq、pydantic_core、Pillow 原生模块导入通过。

首次上传失败为 Node 原生 copyFile 写 SMB 时返回 EIO；不是公证失败。共享盘仍挂载，剩余70.34 GiB，1 MiB 写入/fsync/读回通过。没有足够证据断言是服务端硬件故障或权限问题。提交 `ac331ad64c` 改为1 MiB缓冲流式写入任务专属.part文件，仍保留源文件摘要检查、共享盘完整读回SHA-256校验及原子改名。4项上传相关测试通过，包括跨多个缓冲区与失败残片重试。

沿用原任务，仅重试上传后成功：status=built、uploadStatus=uploaded、error/uploadError均为空。原EIO保存在日志及previousUploadFailure字段，未丢失故障证据。没有重新构建或再次公证。

最终文件：`op-changli-1.0.0-mac-arm64-release-d58a087e-cdc4-4d3a-814d-2046cb012fc3.dmg`，525045195字节，SHA-256 `0a549214b99319bae844a2e47a128558495a349eb78260e67f5a542b6ba8e4ac`。共享盘为 `\\192.168.31.40\Share\private\`，Mac挂载路径 `/Volumes/Share/private/`；本机归档位于构建clone的 `apps/tabtin-electron/dist-app-quick/`。正式客户标识为op-changli，全部使用其原计划，不是此前op-validation测试包。

审阅指出的DMG身份选择边界已由 `a4fce377b8` 修复：DMG与app/Python使用同一个显式专用钥匙串。控制台源码已更新至 `ac331ad64c`，工作分支尚未合入release/main；自动化仍暂停。客户服务登录及业务验收未包含在本次签名/公证/上传验证中。


## Intel 正式版缺少 Python runtime 的定位与修复

用户任务 `0cafc8a1-3f05-4a67-9c92-96ef4cbba77c`：private/260907 固定提交 `62940c5cb5503be3824bcee5771773c42b226241`，mac-x64、1.0.0、release、上传。afterPack 因缺少包内Python manifest失败。向前追日志，真正原因是cryptography 50.0.1没有Intel macOS wheel，pip转源码编译，openssl-sys缺OpenSSL环境失败；旧Community脚本吞掉该错误继续打包。

已核对之前成功Intel测试任务 `59c994be-da4a-477f-a27c-6689af0751ee`：同样cryptography wheel构建失败，只是记录“非致命、无内建python、回落系统python”后继续成功。因此测试包出包成功不代表内置Python完整；此前验收漏了这项，不能把缺运行时继续交付作为本次公证版的修复方式。

PyPI官方发布元数据核对：50.0.1、49.0.0只有Mac arm64 wheel，48.0.1仍有cp311-abi3-macosx_10_9_universal2。提交 `e1d4a285c1` 为Intel构建设置PIP_ONLY_BINARY=cryptography，让pip在满足源码依赖约束的候选中解析兼容wheel，不强制忽略约束或改用ARM库。Community所有Mac构建在Electron编译前先强制准备Python archive和manifest，失败立即停止；ARM缓存保留。

实际预检已解析cryptography48.0.1并成功生成x64归档：sha256 e22d8326d92b11019062df8bf14cdf41adf5537ee0c2266936d0e23b60e7f309，116866349字节。正式包还会按现有流程签署归档内部Mach-O并更新摘要。

同时为Apple Silicon宿主准备Intel文件生成程序：使用x64 Python及Rosetta，在任务独立目录运行原filegen构建脚本；验证Mach-O架构和--help启动后，只缓存tabtin-filegen-darwin-x64，按源目录Git tree与产物SHA校验复用，不覆盖ARM程序。第一次预检任务 `78c9c6ff-1147-4a9e-acc6-bf0f739b2ed4` 的lipo参数顺序错误，被实际运行捕获；提交 `d2958a860d` 改为binary在前，并通过本机实际lipo调用验证。

本地全量37项测试通过。当前重试 `a3e942bd-fa70-4818-9bbd-e316c34d22f1` 沿用原公证版计划，完整签名、公证及上传仍待结果。自动化保持暂停。


### Intel 正式版最终验收结果

重试 `a3e942bd-fa70-4818-9bbd-e316c34d22f1` 已成功：客户“河北长立”，1.0.0，mac-x64，release。Apple提交 `24ddde7a-5e9c-4e23-a84d-1a53c7aa109d` 返回 Accepted；公证票据、最终DMG挂载后地址/身份/严格签名审计均通过，流式共享盘上传及SHA-256校验通过。平台记录status=built、uploadStatus=uploaded；构建clone git status干净。

文件 `op-changli-1.0.0-mac-x64-release-a3e942bd-fa70-4818-9bbd-e316c34d22f1.dmg`，516433065字节，SHA-256 `966ac6fb0496f7a36827e13e552c28a9cbae26254690e18ffcb7bc41fa01000c`。已交付至 `\\192.168.31.40\Share\private\`，本地仍在构建clone的 `apps/tabtin-electron/dist-app-quick/`。

实际验证：Intel文件生成程序架构及--help启动通过；包内92个Python程序已签名/验签；从包内归档提取的Python启动报告x86_64，cryptography48.0.1及其Rust原生绑定导入成功。验证在新ARM Mac的Rosetta环境完成；Intel真机及客户业务登录验收仍由测试执行。本次没有修改或覆盖已经交付的ARM包，没有恢复定时任务。

控制台工作分支最新修复为d2958a860d（依赖准备e1d4a285c1及lipo参数修正），已部署；未合并release/main。


## 打包平台九项界面与任务管理优化

用户将Windows出包实现留到次日，本轮仅实施页面、任务控制和本地空间保留。已部署至新机控制台，工作分支最终提交 `c9efa972ef`；部署前确认无活跃构建或上传，统一token不变，定时任务仍暂停。

| 用户要求 | 实现与验收 |
| --- | --- |
| 日志可刷新 | 日志弹窗新增刷新按钮及更新时间，刷新时保留阅读位置或跟随末尾 |
| 构建中蓝色 | 排队、预检、依赖、构建、审计、取消中使用蓝色状态；浏览器实测rgb(23,105,170) |
| 按钮有反馈 | 刷新、分支刷新、日志刷新、保存、生成计划、提交、取消等有即时禁用/加载状态和完成反馈 |
| 默认上传共享盘 | 新页面默认交付方式upload，仍可手动选择只留本地 |
| 可取消构建 | 排队立即取消；运行任务写持久取消请求，worker停止子进程组并执行源码恢复/释放锁后结束。上传可单独取消，保留已完成本地包 |
| 远程分支筛选 | git ls-remote读取远程分支；前端支持子序列模糊匹配，前缀/包含匹配优先，支持键盘选择。新机实测读取3980条 |
| 记录信息完整 | 显示分支、SHA和提交标题、版本/平台、方式/交付方式、操作人、创建/开始/结束和耗时。新任务完整记录时间；旧记录缺字段显示—，不把更新时间冒充结束时间。旧10条提交标题已从Git对象读取 |
| 最近10条与本地保留 | 页面只请求/展示10条；后台保留小元数据和日志。空闲且无源码锁时清理10条之外、归属于任务的本地安装包、恢复副本及已知构建临时目录，共享盘不动。迟到的活跃任务在机状态区保持日志/取消入口，不计入历史10条 |
| 分支默认空 | 初次加载和刷新后不默认选择分支，明确选择或输入后才可生成计划 |

兼容与边界：旧GET /api/jobs默认仍返回全历史，页面使用可选limit=10；active=1用于活跃任务。公证请求取消只停止本机等待和交付，已提交给Apple的远端处理不保证撤回。共享盘完成原子发布后遇到迟到的取消，保留真实uploaded结果；不删除已发布文件。Windows执行与Windows取消尚未做真实平台验收。

44项测试在本地和新Mac通过，包括真实worker取消Git同步、子孙进程终止、源码锁释放、取消/完成状态边界、旧包路径越界、孤立半成品及两种staging根清理、历史API兼容。浏览器夹具实测默认空分支/上传、模糊筛选及键盘选择、刷新反馈、日志刷新、取消中反馈、十条记录与蓝色状态，控制台无JS错误。

生产检查：recent接口10条，内部历史12条，旧10条均显示提交标题，active=0，retentionError为空。2条较旧记录已标记本地清理；它们没有安装包，当前已有安装包均在保留范围内。共享盘5个既有文件大小和修改时间保持不变。日志/源代码/依赖缓存没有作为旧安装包盲删。
