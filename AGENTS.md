# 文档仓库协作规范

## 仓库定位

本仓库是项目的规划与规范中心，专门保存各业务领域、功能和组件的：

- 产品与技术规划
- 功能、组件、接口、数据和流程规范
- 可交付给开发人员或编码 Agent 的代码执行计划
- 重要技术决策、风险、验收标准和后续变更记录

本仓库默认不存放产品源代码、构建产物、依赖包、临时文件、密钥或真实敏感数据。示例代码只用于解释规范或计划，并应保持最小化。

## 基本原则

1. 默认使用中文编写；代码标识符、协议字段和业界通用术语保留原文。
2. 文档必须区分已确认事实、设计决策、假设和待确认事项，不得把推测写成事实。
3. 先阅读目标目录及其上级目录中的现有文档，再新增或修改内容，避免重复规范和相互冲突。
4. 同一个主题优先维护一份权威文档；需要拆分时，在入口文档中建立清晰索引。
5. 规范描述“要实现什么以及必须满足什么”，执行计划描述“基于当前代码具体怎么实现”。两者不得混写成模糊的愿望清单。
6. 引用代码、接口、配置或外部资料时，必须给出可定位的信息。无法核实时，明确标记为“待确认”。
7. 只修改当前任务涉及的文档，不顺手重写无关内容。

## 推荐目录结构

沿用“业务领域 / 功能或组件”的组织方式：

```text
<业务领域>/
└── <功能或组件>/
    ├── 说明.md
    ├── 规划/
    │   ├── 功能规范.md
    │   ├── 技术方案.md
    │   └── 接口与数据规范.md
    ├── 代码执行计划/
    │   └── YYYY-MM-DD-<中文计划名称>.md
    └── 决策记录/
        └── YYYY-MM-DD-<中文决策主题>.md
```

除 `规划/` 和 `代码执行计划/` 外，其余文件和目录按实际需要创建，不要求为了满足结构而生成空文档。

- `说明.md`：该功能或组件的入口，说明范围、当前状态、负责人（如已知）及文档索引。
- `规划/`：相对稳定的需求、技术、接口、数据、交互和质量规范。
- `代码执行计划/`：针对一次明确交付目标的、可逐项执行和验证的实施计划。
- `决策记录/`：记录影响较大且以后可能需要追溯原因的取舍。

业务领域、功能、组件、目录、文档、规划、执行计划、章节和实施步骤的名称必须使用中文，并做到直白、稳定、可搜索。例如使用 `2026-08-06-元素备份恢复实施计划.md`，不得使用 `backup-plan.md`、`phase-1.md` 或中英文混杂的名称。

以下内容不受中文命名限制：

- `AGENTS.md` 等工具要求的约定文件名
- 代码中的真实文件路径、类名、函数名、配置项、接口路径和字段名
- 无通用中文译名或保留原文更准确的技术术语

正文首次出现英文技术术语时，优先写成“中文说明（英文原词）”。避免使用“新版”“最终版”“其他”“临时”等缺少语义的名称。

## 文档通用元信息

新建主要文档时，在标题后提供以下信息；未知项可写“待确认”，不编造内容：

```markdown
> 状态：草稿 | 评审中 | 已确认 | 已废弃
> 最后更新：YYYY-MM-DD
> 适用范围：涉及的产品、服务、模块或版本
> 关联文档：相对路径链接
```

状态含义：

- `草稿`：内容仍可能发生明显变化。
- `评审中`：方案已成形，正在收集反馈或等待确认。
- `已确认`：可作为实现和验收依据。
- `已废弃`：不再适用；必须注明替代文档或废弃原因，通常不直接删除历史记录。

## 规范文档要求

规范文档应按主题选取下列章节，不适用的章节可省略：

1. 背景与问题
2. 目标
3. 非目标
4. 术语与定义
5. 使用场景或用户流程
6. 功能要求
7. 组件边界与职责
8. 接口、事件与数据模型
9. 状态、权限、异常和边界条件
10. 性能、安全、可靠性、可观测性等非功能要求
11. 兼容性、迁移与回滚要求
12. 验收标准
13. 未决问题

要求使用可验证的表述。例如，不写“性能要好”，应写明适用场景、指标、统计口径和目标值；如果目标值尚未确定，列为未决问题。

涉及接口或数据结构时，应明确：

- 字段名称、类型、是否必填、语义和约束
- 请求、响应、事件或状态转换的示例
- 错误码、失败行为、幂等性和重试规则
- 权限边界及敏感数据处理方式
- 版本兼容策略和变更影响

## 规划文档图解要求

规划文档必须用图解呈现关键关系和行为，不能只依赖大段文字。图解优先使用 Mermaid，以便版本管理、检索和后续修改；只有 Mermaid 无法准确表达时才使用仓库内的图片文件。

每份规划文档至少包含以下两类有效图解：

1. **全景图**：展示参与者、系统边界、组件及外部依赖。
2. **核心流程图**：展示主要业务路径、关键判断、失败分支和最终结果。

涉及多个组件、服务或复杂状态的规划，原则上应包含至少四张图，并根据内容补充：

- 组件架构图或依赖图：说明模块职责、调用方向和边界。
- 时序图：说明跨组件调用顺序、同步与异步行为、异常和重试。
- 状态图：说明对象生命周期、合法转换和失败状态。
- 数据关系图：说明核心实体、主外键或业务关联。
- 权限或数据流图：说明身份、授权边界、敏感数据流向和存储位置。
- 发布或迁移图：说明新旧版本切换、灰度、兼容窗口和回滚路径。

图解必须满足以下质量要求：

- 每张图只回答一个清晰问题，并在图前用一句话说明其目的。
- 节点使用中文业务名称；真实代码标识符可在中文名称后用括号注明。
- 图中的组件、状态、接口和流程必须与正文术语一致。
- 核心正常路径之外，还应体现影响设计的失败分支、异步边界或权限边界。
- 图后补充必要说明，解释图中无法直接表达的约束和关键取舍。
- 不得为了凑数量重复同一信息，也不得使用没有业务含义的装饰性图。

简单规划若确实不适用某类图，可以省略，但仍至少保留全景图和核心流程图；无法满足时，应在文档中明确说明原因。

## 代码执行计划要求

执行计划必须建立在已确认的需求和当前代码事实之上。若代码仓库、分支或关键上下文不可用，应先列出需要核实的内容，不得虚构文件路径、类名、接口或现有行为。

每份计划至少包含：

1. **交付目标**：完成后系统具备什么能力。
2. **依据与现状**：关联规范、相关代码入口、当前行为和已知限制。
3. **范围**：本次包含与明确不包含的内容。
4. **前置条件与依赖**：外部系统、数据、权限、版本或其他任务。
5. **实施步骤**：按依赖顺序编号，写明修改位置、具体改动和该步验证方式。
6. **测试计划**：单元、集成、端到端、回归及必要的人工验证。
7. **发布、迁移与回滚**：开关、数据迁移、兼容窗口、监控和回退办法。
8. **风险与待确认事项**：说明影响、应对方式和确认责任方（如已知）。
9. **完成标准**：可逐项检查的完成定义（Definition of Done）。

实施步骤使用以下粒度：

```markdown
### 1. <中文步骤名称>

- 修改位置：`仓库/相对路径`，相关符号或配置项
- 具体改动：新增、修改或移除什么，以及关键行为
- 依赖关系：需要先完成的步骤或外部条件
- 验证方式：要运行的测试、命令或可观察结果
```

计划中的每一步应能让执行者判断“在哪里改、改什么、为什么改、怎样确认完成”。避免只写“完善后端”“处理异常”“补充测试”等无法直接执行的描述。

## 验收标准写法

验收标准必须客观、可重复验证，优先使用“给定 / 当 / 则”或清单形式：

```markdown
- [ ] 给定 <前置状态>，当 <触发动作>，则 <可观察结果>。
- [ ] 失败场景返回约定错误，且不会产生部分写入或重复副作用。
- [ ] 新旧版本在约定兼容期内可以同时工作。
```

“代码已编写”不等于完成。完成标准通常还应覆盖测试通过、文档同步、兼容性验证、监控就绪以及未决事项已关闭或明确移交。

## 变更与一致性

- 修改规范时，同步检查相关执行计划、接口说明、数据定义和验收标准是否需要更新。
- 修改计划时，不得悄悄改变已确认的需求；若实现约束要求调整规范，先记录差异和原因。
- 文档之间出现冲突时，以状态为“已确认”且适用范围最具体的文档为准；仍无法判断时，将冲突列为待确认事项。
- 使用相对路径链接仓库内文档，并在移动或重命名后修复引用。
- 对重大方向变化保留决策记录，说明背景、候选方案、最终决定、取舍和后果。

## 智能体工作流程

处理文档任务时，按以下顺序执行：

1. 确认目标业务领域、功能或组件，以及任务是新增规范、修订规范还是编写执行计划。
2. 阅读相关入口、上级规范、现有计划和决策记录。
3. 如需依据代码编写计划，检查实际代码和测试，确认文件路径、符号、调用关系及当前行为。
4. 识别信息缺口；能从现有资料核实的先自行核实，无法核实且会影响方案的标为待确认。
5. 在最贴近主题的位置新建或更新文档，并补充入口索引。
6. 完成一致性检查：范围、术语、接口、数据、步骤、测试、验收和文档链接相互对应。
7. 汇报本次修改、关键决策、未决问题及建议的下一步。

## 提交前检查

- [ ] 文档存放位置和名称符合其业务范围。
- [ ] 业务目录、文档标题、计划名称、章节和步骤名称均使用中文。
- [ ] 标题、状态、日期、适用范围和关联文档完整。
- [ ] 目标、非目标和边界清晰，没有隐含扩张范围。
- [ ] 事实、决策、假设和待确认事项已明确区分。
- [ ] 规划至少包含全景图和核心流程图；复杂规划包含足够的架构、时序、状态或数据图。
- [ ] 图解与正文术语、流程、异常和边界一致，不存在装饰性或重复图。
- [ ] 接口、字段、状态和术语在相关文档中保持一致。
- [ ] 执行计划引用的路径和符号已核实，步骤可直接实施。
- [ ] 测试、发布、迁移、回滚、风险和验收标准足够具体。
- [ ] 仓库内链接有效，没有新增无意义的空文件或重复文档。
